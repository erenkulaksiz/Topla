import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    container: {
        width: "100%",
    },
    questionTitleWrapper: {
        width: "100%",
    },
    questionTitle: {
        fontSize: 26,
    }
});